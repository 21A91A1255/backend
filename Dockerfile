FROM node:18.17.1

ARG PORT
ARG DEV_MODE
ARG MONGO_URL

ENV PORT=$PORT
ENV DEV_MODE=$DEV_MODE
ENV MONGO_URL=$MONGO_URL

WORKDIR /app
COPY package.json ./
RUN npm install
COPY . .
EXPOSE 8080
CMD ["npm","run","start"]
